#include <bits/stdc++.h>

using namespace std;

struct Car
{
    string num;
    int hh;
    int mm;
    int ss;
    bool status;
};

int main()
{
    int n, m;
    cin >> n >> m;
    vector<Car> v(n);
    for (auto i = 0; i < n; ++i)
    {
    }
    return 0;
}