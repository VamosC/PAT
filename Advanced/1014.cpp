// #include <bits/stdc++.h>
#include <iostream>
#include <queue>
#include <deque>
using namespace std;

int main()
{
    int n, m, k, q;
    cin >> n >> m >> k >> q;
    deque<int> t;
    for (auto i = 0; i < n; ++i)
    {
        int x;
        cin >> x;
        t.push_back(x);
    }

    return 0;
}